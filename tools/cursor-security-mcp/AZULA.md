Vendored from https://github.com/gurkanfikretgunak/cursor-security (`packages/cursor-security-mcp` v1.1.0).

Cursor loads it via `.cursor/mcp.json`. After clone: `npm install` in this folder (runs `prepare` → `tsc`).
